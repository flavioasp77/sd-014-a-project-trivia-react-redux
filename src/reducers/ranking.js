// import { SET_PLAYER_INFO } from '../actions';

const INITIAL_STATE = {
};

const ranking = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default ranking;
