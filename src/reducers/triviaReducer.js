const INITIAL_STATE = {
  questions: '',
  token: '',
};

const triviaReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default triviaReducer;
