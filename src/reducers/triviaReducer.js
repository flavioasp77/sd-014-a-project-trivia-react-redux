const INITIAL_STATE = {
  questions: '',
  token: '',
};

const triviaReducer = (state = INITIAL_STATE) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default triviaReducer;
