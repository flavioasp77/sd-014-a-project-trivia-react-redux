import React from 'react';

class Feedback extends React.Component {
  render() {
    console.log(localStorage.getItem(user.coins)
    return (
      <header>
        <h1 data-testid="feedback-text">
          points
        </h1>
      </header>
    );
  }
}

export default Feedback;
